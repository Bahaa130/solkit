-- 👑 إضافة عمود الدور (role) لجدول المستخدمين لتخزين صلاحية المدير في قاعدة البيانات
-- يضمن وجود مدير واحد فقط عبر تحديث الصفوف (ترقية واحد وتنزيل البقية)

ALTER TABLE `User` ADD COLUMN `role` VARCHAR(191) NOT NULL DEFAULT 'user';
